

// Recebe valores:
let n1 = Number(prompt('Digite um valor: '))
let n2 = Number(prompt('Digite mais um valor: '))

// Calculos:
let soma = n1 + n2

// Mostra em alerta
alert(`A soma entre ${n1} + ${n2} = ${soma}`)
